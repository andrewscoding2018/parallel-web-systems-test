# Parallel Web Systems API key — get one at https://platform.parallel.ai
# Read server-side only; never exposed to the browser.
PARALLEL_API_KEY=your-key-here

# Optional overrides (sensible defaults live in app/config.py):
# PARALLEL_BASE_URL=https://api.parallel.ai
# PARALLEL_SEARCH_BETA=search-2025-06-01
# PARALLEL_TASK_PROCESSOR=core
# RESULTS_PER_QUERY=10
# FRONTEND_ORIGIN=http://localhost:5173
